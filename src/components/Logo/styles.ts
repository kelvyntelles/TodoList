import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        gap: 8,
    },
    title: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    logoTo: {
        fontSize: 38,
        color: '#4EA8DE',
        fontWeight: 'bold'
    },
    logoDo: {
        fontSize: 38,
        color: '#5E60CE',
        fontWeight: 'bold'
    }
})